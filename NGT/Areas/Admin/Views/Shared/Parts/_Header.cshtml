<nav class="main-header navbar navbar-expand bgtop navbar-dark d-flex justify-content-between align-items-center overflow-visible">

    <div class="justify-content-center align-items-center">
        <a class="otherLink pl-3" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
    </div>

    <div class="justify-content-center align-items-center">
        <div>
            <h1 class="brand">@User.Identity.Name.Split('|')[3] - @ViewBag.Title</h1>
        </div>
    </div>

    <div class="d-flex justify-content-center align-items-center">

        <div class="user-panel image pr-3">
            <img src="@User.Identity.Name.Split('|')[4]" class="img-circle profile-user-img" alt="User Image" />
        </div>

        <ul class="navbar-nav pr-3">
            <li class="nav-item dropdown">
                <a id="dpMenuUser" href="#" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" class="otherLink dropdown-toggle">
                    <span class="hideCollapse">
                        @User.Identity.Name.Split('|')[1]
                    </span>
                </a>

                <ul aria-labelledby="dpMenuUser" class="dropdown-menu border-0 shadow">
                    <li><a id="alteraSenha" ref="@User.Identity.Name.Split('|')[0]" href="#" class="dropdown-item">Alterar senha</a></li>
                </ul>
            </li>
        </ul>

        <div class="sepImg">
            |
        </div>
        <div class="pl-3">
            <a href="@Url.RouteUrl("Admin.LogOut")" class="btnLogout">
                <i class="fas fa-sign-out-alt"></i>
            </a>
        </div>
    </div>


</nav>

<div id="modal" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="TrocaSenha"></div>
<script>
   
$(function () {
            $('#alteraSenha').click(function () {
                $("#modal").load("@Url.RouteUrl("Admin.Usuario.AlteraSenha")", function () {
                    $("#modal").modal();
                });

                $('#modal').html('<div class="c-loader m-4"></div>');

                @*setTimeout(function () {
                    var termo = $('#termo').val();
                    $.get('@Url.Action("ConsultaChamado","Ocorrencias")', { termo: termo }, function (resultado) {
                        $('#result').removeClass("d-flex justify-content-center");
                        $('#result').html(resultado);
                    });
                }, 1500);*@

            });
        });
</script>